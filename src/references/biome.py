from enum import auto
from enum import Enum


class Biome(Enum):
    DESERT = auto()
    FLOWERED = auto()
    GRASS = auto()
    TUNDRA = auto()
    OCEAN = auto()
    SAVANNA = auto()
    SNOW = auto()